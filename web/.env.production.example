# Variables de build para Netlify (Site settings > Environment variables).
# Reemplaza por la URL real de tu backend en Render (incluye /api, sin slash final).
VITE_API_BASE_URL=https://TU-BACKEND.onrender.com/api
